import React from 'react';

export default class AddPlayerPage extends React.Component {
    render() {
        return (
            <section aria-live="polite">
                <h2>Add a Player</h2>
                <p>Add Player Form</p>
            </section>
        );
    }
}